/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            'localhost',
            'https://*.googleusercontent.com'
    ]}
}

module.exports = nextConfig
